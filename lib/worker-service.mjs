import{get,put}from'./store.mjs';import{claim,transition,retry,heartbeat,renewLease}from'./queue.mjs';import{executeJob}from'./platform-v1.mjs';import{prepareExperiment,aggregateExperiment,scheduleAggregation}from'./experiment-service.mjs';import{governProductionRun}from'./governor-service.mjs';
export class WorkerService{
 constructor({workerId=`worker-${crypto.randomUUID()}`,leaseSeconds=60,renewEveryMs=Math.max(100,leaseSeconds*400),crashHook=null}={}){Object.assign(this,{workerId,leaseSeconds,renewEveryMs,crashHook})}
 async tick(){await heartbeat(this.workerId);let q=await claim(this.workerId,this.leaseSeconds);if(!q)return null;let busy=false,renewError=null;const timer=setInterval(async()=>{if(busy)return;busy=true;try{await renewLease(q,this.workerId,this.leaseSeconds)}catch(e){renewError=e}finally{busy=false}},this.renewEveryMs);
  try{q=await transition(q,'running',{actor:this.workerId});await this.crashHook?.('after_claim',q);const jid=q.job_id||q.data.jobId,j=await get(jid);if(!j)throw Error('queued_job_missing');let experiment;if(j.data.operation==='prepare_experiment')experiment=await prepareExperiment(q.company_id,j.data.experimentId);if(j.data.operation==='aggregate_experiment')experiment=await aggregateExperiment(q.company_id,j.data.experimentId);if(experiment){q=await transition(q,'evaluating',{actor:this.workerId});q=await transition(q,'completed',{actor:this.workerId});j.state='completed';j.data.status='completed';await put(j,{expectedVersion:j.version});return{queue:q,experiment}}const run=await executeJob(q.company_id,jid,{shadow:Boolean(j.data.shadow),revisionId:j.data.organizationRevisionId});if(renewError)throw renewError;if(run.state==='waiting_for_approval'){q=await transition(q,'waiting_for_approval',{actor:this.workerId,evidenceIds:run.data.traceIds,reason:'approval_required'});return{queue:q,run}}if(run.state==='uncertain'){q=await transition(q,'uncertain',{actor:this.workerId,evidenceIds:run.data.traceIds,reason:'external_side_effect_outcome_unknown'});return{queue:q,run}}q=await transition(q,'evaluating',{actor:this.workerId,evidenceIds:run.data.traceIds});q=await transition(q,run.state==='completed'?'completed':'failed',{actor:this.workerId});let governor=null;if(j.data.experimentId)await scheduleAggregation(q.company_id,j.data.experimentId);else governor=await governProductionRun(q.company_id,run.id);return{queue:q,run,governor}}
  catch(e){if(e.code==='SIMULATED_CRASH')throw e;q=await retry(q,e.message,{uncertain:Boolean(e.uncertainSideEffect)});return{queue:q,error:e.message}}finally{clearInterval(timer);await heartbeat(this.workerId)}}
}
