import{get,put}from'./store.mjs';import{claim,transition,retry,heartbeat,renewLease}from'./queue.mjs';import{executeJob}from'./platform-v1.mjs';import{prepareExperiment,aggregateExperiment,scheduleAggregation,automaticallyPromoteExperiment}from'./experiment-service.mjs';import{governProductionRun}from'./governor-service.mjs';
import{advanceOperatingSession,replanOperatingSession,scheduleReplan,ensureConfiguredRuntimeProviders}from'./universal.mjs';
export class WorkerService{
 constructor({workerId=`worker-${crypto.randomUUID()}`,leaseSeconds=60,renewEveryMs=Math.max(100,leaseSeconds*400),crashHook=null}={}){Object.assign(this,{workerId,leaseSeconds,renewEveryMs,crashHook})}
 async tick(){await heartbeat(this.workerId,null,{status:'polling'});let q=await claim(this.workerId,this.leaseSeconds);if(!q)return null;await heartbeat(this.workerId,q.id,{status:'working',queueState:q.state});let busy=false,renewError=null;const timer=setInterval(async()=>{if(busy)return;busy=true;try{await renewLease(q,this.workerId,this.leaseSeconds)}catch(e){renewError=e}try{await heartbeat(this.workerId,q.id,{status:'working',queueState:q.state,leaseRenewedAt:new Date().toISOString()})}catch{}finally{busy=false}},this.renewEveryMs);
  try{q=await transition(q,'running',{actor:this.workerId});await this.crashHook?.('after_claim',q);const jid=q.job_id||q.data.jobId,j=await get(jid);if(!j)throw Error('queued_job_missing');let experiment,promotion=null;if(j.data.operation==='prepare_experiment')experiment=await prepareExperiment(q.company_id,j.data.experimentId);if(j.data.operation==='aggregate_experiment'){experiment=await aggregateExperiment(q.company_id,j.data.experimentId);promotion=await automaticallyPromoteExperiment(q.company_id,j.data.experimentId);if(promotion?.action==='PROMOTE'){const sources=await Promise.all((experiment.data.sourceJobIds||[]).map(get));const sessions=[...new Set(sources.map(x=>x?.data?.operatingSessionId).filter(Boolean))];for(const sessionId of sessions)await scheduleReplan(q.company_id,sessionId,{reason:'A candidate organization passed the evidence gates and was promoted.',sourceRef:`promotion:${promotion.decisionId}`,activate:true,evidenceIds:[promotion.decisionId]})}}if(experiment){q=await transition(q,'evaluating',{actor:this.workerId});q=await transition(q,'completed',{actor:this.workerId});j.state='completed';j.data.status='completed';await put(j,{expectedVersion:j.version});return{queue:q,experiment,promotion}}if(j.data.operation==='advance_operating_session'||j.data.operation==='replan_operating_session'){await ensureConfiguredRuntimeProviders(q.company_id);const session=j.data.operation==='advance_operating_session'?await advanceOperatingSession(q.company_id,j.data.operatingSessionId,{singleStage:true}):await replanOperatingSession(q.company_id,j.data.operatingSessionId,j.data.replanRequestId);q=await transition(q,'evaluating',{actor:this.workerId});q=await transition(q,'completed',{actor:this.workerId});j.state='completed';j.data.status='completed';await put(j,{expectedVersion:j.version});return{queue:q,session}}const run=await executeJob(q.company_id,jid,{shadow:Boolean(j.data.shadow),revisionId:j.data.organizationRevisionId});if(renewError)throw renewError;if(run.state==='waiting_for_approval'){q=await transition(q,'waiting_for_approval',{actor:this.workerId,evidenceIds:run.data.traceIds,reason:'approval_required'});return{queue:q,run}}if(run.state==='uncertain'){q=await transition(q,'uncertain',{actor:this.workerId,evidenceIds:run.data.traceIds,reason:'external_side_effect_outcome_unknown'});return{queue:q,run}}q=await transition(q,'evaluating',{actor:this.workerId,evidenceIds:run.data.traceIds});q=await transition(q,run.state==='completed'?'completed':'failed',{actor:this.workerId});let governor=null;if(j.data.experimentId)await scheduleAggregation(q.company_id,j.data.experimentId);else governor=await governProductionRun(q.company_id,run.id);if(governor?.data?.action==='REPLAN_STRATEGY'&&j.data.operatingSessionId)await scheduleReplan(q.company_id,j.data.operatingSessionId,{reason:governor.data.reason,sourceRef:`governor:${governor.id}`,activate:false,evidenceIds:governor.data.evidenceIds});return{queue:q,run,governor}}
  catch(e){if(e.code==='SIMULATED_CRASH')throw e;q=await retry(q,e.message,{uncertain:Boolean(e.uncertainSideEffect)});return{queue:q,error:e.message}}finally{clearInterval(timer);try{await heartbeat(this.workerId,null,{status:'idle'})}catch{}}}
}
