# 90-second judge verification

1. Run **Invoice controls**. Baseline should fail repeated duplicate cases.
2. Open a failed evidence row and inspect the raw role/tool trace.
3. Inspect the diagnosis and competing restructures.
4. Confirm only a candidate rerun that passes the same operating contract is promoted.
5. Inject operating drift. The existing company should fail a different integrity condition and restructure again.
6. Switch to **Support privacy** and repeat. A different capability should be learned.
7. Open **Evidence** and export the machine-readable run bundle.
8. Import a custom mission JSON to verify the product is not limited to a single fixed screen.
9. Show the real AO dashboard/session total separately in the demo.
10. If Neatlogs credentials are configured, inspect the same run as a nested workflow/tool trace in Neatlogs.
